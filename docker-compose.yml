version: '3'
services:
  api-gateway:
    build: .
    ports:
      - "3031:3031"
  frontend:
    image: nginx:1.13-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api-gateway
    ports:
      - 80:80
